// Vendors
import styled from 'styled-components';

const DialogPageStyled = styled.div`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: ${({ theme }) => theme.spaces.space16};
`;

export { DialogPageStyled };
