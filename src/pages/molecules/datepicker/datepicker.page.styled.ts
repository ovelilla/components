// Vendors
import styled from 'styled-components';

const DatepickerPageStyled = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${({ theme }) => theme.spaces.space16};
`;

export { DatepickerPageStyled };
