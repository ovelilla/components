// Vendors
import styled from 'styled-components';

const TitlePageStyled = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${({ theme }) => theme.spaces.space16};
`;

export { TitlePageStyled };
