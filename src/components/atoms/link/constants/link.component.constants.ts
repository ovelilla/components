const DEFAULT_BOLD = false as const;
const DEFAULT_HREF = '#' as const;
const DEFAULT_UNDERLINE = false as const;
const DEFAULT_UPPERCASE = false as const;

export { DEFAULT_BOLD, DEFAULT_HREF, DEFAULT_UNDERLINE, DEFAULT_UPPERCASE };
