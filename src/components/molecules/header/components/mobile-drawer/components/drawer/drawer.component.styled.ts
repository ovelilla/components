// Vendors
import styled from 'styled-components';

const DrawerComponentStyled = styled.div`
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.4);
`;

export { DrawerComponentStyled };
