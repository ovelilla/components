enum DatepickerDayComponentStatesEnum {
  DEFAULT = 'default',
  DISABLED = 'disabled',
  DISABLED_TODAY = 'disabled-today',
  OUTSIDE = 'outside',
  OUTSIDE_SELECTED = 'outside-selected',
  OUTSIDE_TODAY = 'outside-today',
  SELECTED = 'selected',
  TODAY = 'today',
}

export { DatepickerDayComponentStatesEnum };
