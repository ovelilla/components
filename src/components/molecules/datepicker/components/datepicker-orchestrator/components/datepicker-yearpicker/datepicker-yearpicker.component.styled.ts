// Vendors
import styled from 'styled-components';

const DatepickerYearpickerComponentStyled = styled.div`
  border-top: ${({ theme }) => `${theme.borders.border1} solid hsl(${theme.colors.border})`};
  flex: 1;
  overflow: auto;
`;
export { DatepickerYearpickerComponentStyled };
